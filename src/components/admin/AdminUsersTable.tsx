'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminUsersTable() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Users Table</CardTitle>
        <CardDescription>User management table will be implemented here</CardDescription>
      </CardHeader>
      <CardContent>
        <div className="text-center py-8">
          <p className="text-gray-600">Users table component will be implemented here</p>
          </div>
      </CardContent>
    </Card>
  );
}
